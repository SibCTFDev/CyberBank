import {Grid2 as Grid, Button, Divider} from "@mui/material";


function ProductCard(props) {
    const {id, image_id, description, price} = props;

    return (
        <Grid
            container
            alignItems="center"
        >
           
        </Grid>
    );
}

export default ProductCard;